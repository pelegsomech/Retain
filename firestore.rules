rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access by default - only admin SDK can access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
